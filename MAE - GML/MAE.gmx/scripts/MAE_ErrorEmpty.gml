///MAE_ErrorEmpty()

/**
 * Description:
 *   Indicates if the error stack is empty.
 *
 * Arguments:
 *   [N/A]
 *
 * Returns:
 *   1 if the stack is empty, otherwise 0.
 */

return external_call(global.MADX9_ErrorEmpty);
