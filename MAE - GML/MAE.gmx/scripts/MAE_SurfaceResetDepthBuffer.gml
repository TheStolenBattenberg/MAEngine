///MAE_SurfaceResetDepthBuffer()

/**
 * Description:
 *   Resets the depth buffer
 *
 * Arguments:
 *   [N/A]
 *
 * Returns:
 *   0 on Error, 1 on success.
 */

return external_call(global.MADX9_SurfaceResetDepthBuffer);
