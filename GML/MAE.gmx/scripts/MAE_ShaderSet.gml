///MAE_ShaderSet(shader_index);

/**
 * Description:
 *   Sets a shader.
 *
 * Arguments:
 *   [1] - Index to the shader created by MAE_ShaderCreate
 *
 * Returns:
 *   0 on Error, 1 on success.
 */

return external_call(global.__MAE_ShaderSet, argument0);
