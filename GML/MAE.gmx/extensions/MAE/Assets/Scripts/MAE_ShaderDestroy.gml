///MAE_ShaderDestroy(shader_index);

/**
 * Description:
 *   Destroys a shader.
 *
 * Arguments:
 *   [1] - Index to the shader created by MAE_ShaderCreate
 *
 * Returns:
 *   0 on Error, 1 on success.
 */

return external_call(global._MAE_ShaderDestroy, argument0);
