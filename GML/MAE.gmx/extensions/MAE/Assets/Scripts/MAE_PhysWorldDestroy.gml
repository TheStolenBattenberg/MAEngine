///MAE_PhysWorldDestroy();

/**
 * Description:
 *   Destroys the physics world and everything in it. Shapes are not destroyed.
 *
 * Arguments:
 *   [-]
 *
 * Returns:
 *   Success
 */

return external_call(global._MAB_WorldDestroy);
