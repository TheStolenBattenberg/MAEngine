///MAE_PhysWorldDebugDraw();

/**
 * Description:
 *   Draws a debug wireframe representation of the physics world, including physics bodies, constraints, and collision points. Call this function in a draw event.
 *
 * Arguments:
 *   [-]
 *
 * Returns:
 *   Success
 */

return external_call(global._MAB_WorldDebugDraw);
