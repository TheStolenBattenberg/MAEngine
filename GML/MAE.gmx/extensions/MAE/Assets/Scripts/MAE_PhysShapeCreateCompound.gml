///MAE_PhysShapeCreateCompound();

/**
 * Description:
 *   Creates an empty shape that can have other shapes added to it as children. See MAE_PhysShapeAddChild().
 *
 * Arguments:
 *   [-]
 *
 * Returns:
 *   Shape ID
 */

return external_call(global._MAB_ShapeCreateCompound);
