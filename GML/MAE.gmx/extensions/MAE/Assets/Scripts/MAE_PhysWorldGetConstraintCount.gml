///MAE_PhysWorldGetConstraintCount();

/**
 * Description:
 *   Returns the number of constraints that are in the physics world.
 *
 * Arguments:
 *   [-]
 *
 * Returns:
 *   Real
 */

return external_call(global._MAB_WorldGetConstraintCount);
