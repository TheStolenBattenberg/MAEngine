///MAE_PhysWorldExists();

/**
 * Description:
 *   Checks if the physics world exists.
 *
 * Arguments:
 *   [-]
 *
 * Returns:
 *   1 if the world exists. 0 if it does not.
 */

return external_call(global._MAB_WorldExists);
